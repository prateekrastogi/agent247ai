'use client';

import ScrollHandler from './ScrollHandler';

const BookDemoButton = () => {
  return (
    <>
      <ScrollHandler onBookDemoClick={() => { }} />
    </>
  );
};

export default BookDemoButton;
