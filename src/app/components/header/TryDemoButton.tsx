'use client';

import ScrollHandler from './ScrollHandler';

const TryDemoButton = () => {
  return (
    <>
      <ScrollHandler onTryDemoClick={() => { }} />
    </>
  );
};

export default TryDemoButton;
